# Função principal do algoritmo
def main():
    # Passo 1: Criação do vetor e leitura das notas
    notas = []
    for i in range(4):
        nota = float(input(f"Digite a nota {i+1}: "))
        notas.append(nota)

    # Passo 2: Cálculo da média das notas
    media = (notas) / (notas)

    # Passo 3: Impressão da média
    
    
